# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## Project Overview

Jeed (**J**unbeom f**eed** handler) — KRX UDP 멀티캐스트 · FIX 4.4 · 크립토 거래소
WebSocket 을 받아 **고정 레이아웃 와이어 레코드**로 만들어 공유 메모리로 넘기는 피드 핸들러. 소비자(fractal-engine OMS)와
**별도 프로세스**로 돈다. HFT 경로이므로 성능이 중요하다.

**범위 밖:** 주문(KRX FEP/HOA), 오더북, 정규화(`SnapshotData` 변환), fractal-engine 수정.
피드 핸들러는 무상태다 — 디코드 + 필터 + 정규화만 하고 북을 갖지 않는다.

설계 근거는 [`documents/feed_handler.md`](documents/feed_handler.md), 작업 목록은
[`documents/todo.md`](documents/todo.md).

## Project Structure

```
crates/
  jeed-wire/   와이어 레코드 ABI. 의존성 0. 소비자가 의존하는 유일한 것
  jeed-convert/ 고정폭 ASCII 수치 파서 (SWAR). fractal-engine 에서 포팅
  jeed-shm/    named mapping(Win32) / shm_open(POSIX) + SPSC 링 (producer/consumer)
  jeed-krx/    KRX UDP 수신 + 전문 디코더 → WireRecord
  jeed-fix/    FIX 4.4 프로토콜 → MdMessage → WireRecord
  jeed-crypto/ 크립토 WebSocket JSON → WireRecord
               (binance·upbit·bithumb·okx·bybit·bitget·gate·kucoin)
               + recv/ WS·TLS 수신부 — 워크스페이스의 유일한 외부 의존(rustls)
  jeed/        바이너리. conf(TOML 리더 직접 씀)·코어 핀·세그먼트·시그널·리포트 배선
               + src/bin/krx.rs → `jeed-krx`, src/bin/crypto.rs → `jeed-crypto`.
               fix 바이너리는 같은 배선 위에 아직 없다(어댑터가 없어서)
documents/     설계·표준서·제도 문서
conf/          채널 테이블(생성) + 배포 설정(수기)
```

`tests/` 구조는 `src/` 구조를 그대로 따라간다. `src/` 에 모듈을 추가하면 같은 상대 경로에
테스트 파일을 만든다. 모듈 파일은 `foo.rs` 옆에 `foo/` 를 두지 않고 **`foo/mod.rs`** 로 쓴다.

카고는 `tests/*.rs` 만 테스트 크레이트로 잡으므로, 트리를 갖는 테스트는
`tests/<name>/main.rs` 를 루트로 둔다 (`tests/decode/main.rs`). 그래야 `src/decode/` 와
같은 모양이 되고 전문 빌더를 파일마다 복사하지 않는다.

### 거래소를 크레이트로 나누지 않는다

`jeed-crypto` 하나에 `binance/` `upbit/` … 를 둔다. WebSocket + JSON 은 **프로토콜**이고
거래소는 그 방언이다 — 키 이름과 실린 필드가 다를 뿐, 스캔하는 법·소수 문자열을 정수로
만드는 법·북을 `QuotePayload` 에 채우는 법은 같다. 거래소마다 크레이트를 파면 `json.rs` 가
다섯 벌 생겨서 따로 논다 (아래 "시장 모듈은…" 과 같은 논리).

세로로 나누는 기준은 **거래소가 아니라 시장**이다. `binance::spot` 과 `binance::futures` 는
체결 스트림 이름부터 다르고(`@trade` vs `@aggTrade`) 북 봉투도 다르므로 나눈다.

세로로 나누는 기준이 **시장**이라는 건 거래소마다 다르게 떨어진다:

| | 모듈 | 베뉴 바이트 | 이유 |
|---|---|---|---|
| binance | `spot` / `futures` | 2 | 전문이 다르다(`@trade` vs `@aggTrade`) **그리고** `BTCUSDT` 가 충돌 |
| bybit | 하나 | 2 | 전문이 같다. `BTCUSDT` 는 충돌하므로 베뉴만 가른다 |
| bitget | 하나 | 2 | 바이비트와 같다. `instType` 만 다르고 `BTCUSDT` 는 충돌한다 |
| okx | 하나 | 1 | 전문도 같고 `instId` 가 이미 시장을 가른다(`BTC-USDT-SWAP`) |
| upbit · bithumb | 하나씩 (빗썸은 업비트 것을 재수출) | 2 | 현물뿐. 같은 `KRW-BTC` 가 다른 북이라 베뉴는 갈라야 한다 |
| gate | 하나 (현물) | 1 | 현물만 가져왔다. 무기한도 `BTC_USDT` 라 그때 두 번째 바이트가 필요하다 |
| kucoin | `spot` / `futures` | 2 | 전문이 완전히 다르다. 심볼은 안 겹치지만(`XBTUSDTM` vs `BTC-USDT`) 그건 규약이 아니라 작명 습관이다 |

**모듈 수와 베뉴 바이트 수는 다른 질문이다.** 모듈은 *전문이 갈리느냐*, 베뉴는
*`(venue, symbol)` 이 충돌하느냐. 바이비트가 그 둘이 어긋나는 자리다.

### 시장 모듈은 전문이 갈리는 지점에서만 나눈다

`derivative` 는 주식파생과 지수파생을 나누지 않는다 — 헤더·레벨·체결 블록이 전부 같고
**호가 단수와 가격 형식만** 다르다. 그 둘은 함수 두 개(`depth_for_product_group`,
`extract::derivative_price`)로 갈리므로, 모듈을 쪼개면 같은 오프셋이 두 벌 생겨서 따로 논다.

증권은 반대로 진짜 전문이 갈린다:

| | 전문 | 상품군 |
|---|---|---|
| `securities` | 시장 공통 바닥층 + `A3` 0004 체결 | — |
| `stock` | `B6` 0002 590B (LP 없음) | `01S` `01Q` `01X` |
| `etf` | `B7` 0003 830B (LP잔량 포함) | `02S` ELW · `03S` ETF · `04S` ETN · `05S` 수익증권 |

`B603S` 도 `B701S` 도 존재하지 않는다 — LP 유무로 깨끗하게 갈린다. 체결은 시장 전체가
전문 하나라 `securities` 에 한 번만 두고 두 모듈이 가리킨다.

## Build Commands

```bash
cargo build
cargo test
cargo check
```

## KRX 전문을 다룰 때 반드시 아는 것

### 정보분배일련번호는 **공백일 수 있다**

`[5:13]` 8바이트. 스페이스로 채워져 오는 경우가 있다. 표준서가 "시세 : 종목별 보드별 부여
(※ **대용량 서비스에서 제공**)" 이라고 단서를 달고 있고, 그 단서가 **전문마다 다르게 걸린다.**

측정된 사실:

| 전문 | 상태 |
|---|---|
| `B606F` (파생 우선호가) | 2026-04-01 까지 100% 공백 → 05-21 이후 채워짐 |
| `V103F` (가격제한폭 확대) | **20260731 에도 공백** |
| `V101F` / `V102F` | 채워짐 |

**공백을 0 으로 파싱하면 매 메시지가 갭으로 보인다.** 공백은 "갭 없음" 이 아니라
**"측정 불가"** 다. `Option`/센티널로 구분해서 다뤄야 하고, 순서의 권위로 쓰면 안 된다
(순서는 `producer_seq`, 시각은 `recv_ns`). 애초에 이 번호는 (종목 × 보드) 단위라
채널 liveness 신호로도 못 쓴다 — 종목이 조용하면 번호도 멈춘다.

### 표준서 인터페이스정의서의 오프셋 컬럼은 **끝 오프셋**이다

시작 오프셋으로 읽으면 필드가 한 칸씩 밀린다. 조용히 틀리므로 (밀린 채로 파싱돼서 값이
나오긴 한다) 전문 레이아웃을 옮길 때마다 확인한다.

**손으로 세지 않는다.** `documents/krx/layouts.md` 가 `start = 누적 - 길이` 를 풀어 놓은
표이고, 디코더는 그걸 베낀다. 표준서 버전이 오르면 `tools/dump_krx_layout.py` 를 다시 돌린다.

### 전문 끝은 `0xFF`, 길이는 전문마다 고정

`KrxDataParse::validate` 패턴 — **길이 + 종료키워드를 먼저 보고** 그 다음에 필드를 읽는다.
FIX 의 `BodyLength` + `CheckSum` 과 같은 자리다.

### 값이 없을 때 공백이 아니라 `000000.00` 이 오기도 한다

동적상하한가(`G7` `[154:163]`/`[163:172]`)는 제도 미적용 종목(원월물·선물스프레드 등)에
공백이 아니라 `000000.00` 을 싣는다. "값 0" 과 "미적용" 이 구별되지 않으므로 유효 플래그로
결론을 실어 보낸다.

### 호가 단수 — 거래소 원장과 시세 전문은 다르다

**상품마다 거래소가 갖는 호가 단수가 다르다** (지수선물·상품선물과 주식선물이 서로 다르다).
하지만 시세 전문은 **5단 아니면 10단** 둘뿐이고, 깊은 쪽은 실제로 그만큼 쓰지 않는다.
그래서 `WIRE_MAX_DEPTH = 10` 으로 충분하다.

특히 **주식선물(`04F`)은 원장이 10단이어도 시세는 5단으로 잘라서 온다.** 그 이상은 쓰지 않는다.
`B604F` / `G704F` 는 **5단 전문**(324B / 431B)으로 받는다.

표준서 두 개가 이 지점에서 다르게 적혀 있으니 헷갈리지 말 것:

| 문서 | `B604F` |
|---|---|
| 정보분배 v1.341 인터페이스목록 | `IFMSRPD0035` (10단 554B) **한 곳에만** |
| 송신채널 v1.26 전용선 시트 | 5단 목록과 10단 목록(`- 주식선물만`)에 **양쪽 다** |

실제로 오는 건 5단이다. 시세가 10단으로 오는 건 **주식옵션(`05F`)과
개별주식 위클리옵션(`18F`)** 뿐이다.

```
depth_for(trcode):  05F, 18F → 10단        나머지 파생 → 5단
```

`04F` 를 10단으로 읽으면 324B 전문에 554B 레이아웃을 대는 것이라 프레임 검사(길이 불일치)에
걸린다 — 조용히 틀리진 않지만 **한 건도 못 받는다**. pcap 최다 코드가 `B604F`(2,690만 건,
전체의 40%)이므로 여기가 틀리면 수신량의 절반이 사라진다.

그리고 **레코드의 `depth` 는 채널 단수가 아니라 잔량이 있는 레벨 수**다. 책 끝을 넘는 레벨은
공백이 아니라 0 으로 채워 오므로 세어서 싣는다.

### 가격의 소수점 자리수는 전문이 아니라 **종목**이 정한다

파생 실시간 시세 가격은 전부 9바이트인데 안이 세 가지다 (참고-가격표시정보):

| 상품 | 형식 | 소수 |
|---|---|---|
| 주식선물·주식옵션·상품파생 (`04F` `05F` `07F` `10F` `18F`) | `[부호][정수 8]` | 0 |
| 3개월무위험지표금리선물 (`KR4169`/`KR4A69`) | `[부호][정수 4][.][소수 3]` | 3 |
| 그 외 파생 | `[부호][정수 5][.][소수 2]` | 2 |

**상품군만으로는 안 갈린다** — 3개월무위험지표금리선물은 국채/금리파생과 같은 `06F` 인데
형식이 다르다. 그래서 상품군을 먼저 보고, 남는 것만 종목코드 접두사로 가른다
(`jeed_krx::extract::derivative_price`). 비교 몇 번이면 끝나고 종목별 표는 필요 없다.

부호는 양수 `'0'`, 음수 `'-'` (스프레드 호가에만 음수가 온다).
증권·채권은 11바이트 `[부호][미사용][유효숫자 9]` 인데, 미사용 자리가 `'0'` 이라 같은
파서로 읽힌다.

### 전문을 훑어서 소수점을 찾지 않는다

한때 9바이트를 한 번 훑어 `.` 위치를 찾고 값과 자리수를 `Decimal { value, decimals }` 로
같이 들고 다녔다. 지금은 안 그런다:

- 파싱은 `jeed-convert` — 필드를 u64/u128 로 통째로 싣고, 소수점을 마스크·시프트로 지우고,
  자릿수 검증을 비트 연산 한 번으로 하고, 8자리씩 접는다. 바이트마다 분기하지 않는다.
- **스케일은 값이 아니라 리더(reader)의 속성이다.** 종목이 정하는 것이고 한 전문은 한 종목
  것이므로, 리더를 한 번 고르면 전문 전체의 스케일이 정해진다. 레코드 헤더의 `price_scale`
  에 한 번만 찍고 값과 같이 다니지 않는다.

**리더를 잘못 고르면 반드시 실패해야 한다.** 형식마다 `.` 가 다른 자리에 있고 나머지는
숫자여야 하므로, 대부분의 오선택은 알아서 깨진다. 단 하나 — **소수점 있는 리더를 점 없는
필드에 대면 조용히 그 자리 한 바이트를 지운다** (`000012345` → `1245`). 그래서 가격은
`to_i64` 가 아니라 `to_i64_checked` 로 읽는다. `field::price` 가 이미 그렇게 한다.

### 공백 판정은 파서 에러로 대신하지 않는다

공백도 파서에서 `InvalidDigit` 로 튕긴다. 하지만 "거래소가 스페이스를 보냈다" 와 "바이트가
깨졌다" 는 반응이 정반대다 — 앞은 정상, 뒤는 전문을 버려야 한다. 그래서 `field` 의 리더들은
`is_blank` 를 **먼저** 보고 `None` 을 돌려준다. 에러를 삼켜서 `None` 으로 바꾸지 않는다.

### 전문 헤더는 하나가 아니라 셋이다

표준서가 인터페이스마다 표를 따로 그려서 공통부가 잘 안 보인다. 실제로는:

| 모양 | 길이 | 쓰는 곳 | 다른 점 |
|---|--:|---|---|
| A | 47B | 파생·증권 `B6`/`B7`/`A3`/`G7` | 기준형 |
| B | 41B | 채권 `B6`/`A3`/`G7` | 정보분배종목인덱스 없음 |
| C | 33B | 파생 `V1`/`Q2` | 세션ID 없음, 인덱스가 종목코드 뒤 |

A 는 `decode::common::header`, C 는 `decode::derivative::limit_header` 다. 셋 다 같은
`Header` 구조체를 채우므로 그 아래로는 한 타입만 본다.

### 파싱 실패 시 출력 버퍼를 부분 갱신하지 않는다

절반만 채워진 레코드가 링에 나가면 소비자가 그걸 유효한 시세로 읽는다.

## 크립토를 다룰 때 반드시 아는 것

### 심볼은 12바이트에 안 들어간다 — 그래서 헤더 식별자가 24바이트다

`1000000MOGUSDT`(14), OKX 옵션 `BTC-USD-240329-70000-C`(22). `RecordHeader.symbol` 은
`[u8; 24]` 이고 KRX ISIN 은 앞 12바이트만 쓰고 나머지는 `NUL` 이다. 헤더가 48→64B 로
커졌지만 **레코드는 640B 그대로** — 꼬리 패딩이 48→32 로 줄었을 뿐이다.

패딩이 공백이 아니라 `NUL` 인 이유: 0으로 채운 슬롯이 계속 0으로 읽혀야 하고, 심볼 안에
`NUL` 을 넣는 거래소가 없어서 꼬리 판정이 모호하지 않다.

### 스팟과 선물은 다른 `Venue` 다

`BTCUSDT` 가 한쪽에선 현물 페어고 한쪽에선 무기한선물이다. 와이어 식별자가
`(venue, symbol)` 이므로 베뉴 바이트를 공유하면 **소비자가 두 종목의 북을 합친다.**
`BinanceSpot = 3` / `BinanceFutures = 4` 로 가른다. COIN-M 이 오면 세 번째 바이트다.

### 소수는 자르지 말고 튕겨야 한다

`DynamicExtractor::to_i64` 는 **조용히 자른다** (`0.000015` @ 2자리 → `0`). 고정폭 KRX
필드에선 그게 표준서가 정의한 동작이지만, 크립토는 **거래소가 자릿수를 고르므로** 잘린 값이
그럴듯한 다른 값이 된다. 그래서 크립토 경로는 `to_i64_exact` / `to_u64_exact` 를 쓴다 —
스케일이 못 담는 자리가 0이 아니면 `ParseErr::Precision`.

정상 데이터는 항상 tickSize 배수를 전체 자릿수로 패딩해서 오므로(`"3.84410000"`) 뒤가 다
`0` 이고 비교 한 번이다. 0이 아니게 오면 그건 `exchangeInfo` 의 precision 이 바뀐 것이니
프레임을 버리는 게 맞다. (jeed-fix 의 `FixError::Precision` 과 같은 규칙.)

### 델타는 유일하게 자가치유가 아니다

다른 모든 kind 는 직전 것을 **대체**하므로 링 유실이 한 순간의 낡음으로 끝난다. 델타는
**수정**이라 하나를 놓치면 소비자 북이 영영 틀린다. 그래서:

- 갱신ID 사슬(`U`/`u`, USD-M 은 `pu` 까지)을 **손대지 않고** 그대로 싣는다. 갭 탐지는
  소비자 몫이다 — 북을 가진 쪽만 버퍼링·재적용을 할 수 있다
- 레코드가 담는 것보다 변경 레벨이 많으면 **자르지 않고 프레임을 버린다.**
  32단(`WIRE_MAX_DELTA_LEVELS`, 양쪽 공유)을 넘으면 `CryptoError::DeltaOverflow`.
  40개 중 32개를 실어 보내면 소비자는 그걸 완전한 것으로 받아들이고 나머지 8개를 영영
  다시 받지 못한다. 버리면 사슬에 구멍이 나고 소비자가 리싱크한다 — 링 유실용으로 이미
  있어야 하는 장치다
- 스냅샷은 반대로 **자른다.** 5,000단을 10단으로 줄인 건 얕은 북이지 틀린 북이 아니다

### 부분 북(`@depth10`)은 스냅샷이지 델타가 아니다

USD-M 의 `@depth10@100ms` 는 `depthUpdate` 봉투에 `U`/`u`/`pu` 를 달고 오지만 내용은
**매번 상위 10단 전체**다. 이걸 델타로 넣으면 소비자가 완전한 북을 자기 북에 *적용*한다.
**프레임만 봐서는 구별이 안 된다** — 어느 스트림을 구독했는지는 호출자만 안다.

### 첫 바이트 키 디스패치는 **바이낸스에만** 쓴다

`json::next_key` 는 키의 첫 바이트만 돌려준다. 그게 되는 건 바이낸스 전문에서 첫 바이트가
겹치는 키가 없기 때문이지 JSON 의 성질이어서가 아니다. 업비트는 `ask_price`/`ask_size`,
바이비트 봉투는 `topic`/`type`/`ts`, OKX 봉투는 `arg`/`action` 이 겹친다. 나머지 넷은
**`json::next_field`** (키 전체)를 쓴다 — 슬라이스 비교 한 번 더 드는 대신 조용히 틀리지
않는다. 애매하면 `next_field` 다. 거래소가 겹치는 키를 나중에 추가하면 첫 바이트 디코더는
경고 없이 다른 필드를 읽기 시작한다.

### 봉투는 부분 슬라이스로 뜯는다

두 스캐너 모두 **객체 깊이를 세지 않는다.** 각 분기가 자기 값을 건너뛰는 것에 기대어 한
레벨에 머무를 뿐이다. 바이낸스 프레임은 평평해서 그걸로 된다. 나머지는
`{"topic":…,"data":{…}}` 처럼 봉투가 있으므로 `json::object_at` / `objects_at` 으로 안쪽
바이트를 잘라 **독립된 메시지처럼** 걷는다. 부분 슬라이스는 자기 객체 밖으로 못 나간다 —
평평한 걷기가 필요로 하는 성질이 그거다.

### 따옴표 없는 숫자가 온다 (업비트·빗썸)

`"trade_price":52430000.0`. 나머지 거래소는 전부 문자열이다. `json::parse_scalar_bytes` 가
양쪽을 같은 숫자 바이트로 돌려주므로 디코더를 두 벌 만들지 않는다.

그리고 **따옴표 친 정수에 `parse_u64` 를 쓰면 안 된다.** 첫 비숫자에서 멈추는데 그게 닫는
따옴표라, 다음 스캔이 그 따옴표를 키의 여는 따옴표로 읽는다. OKX 는 한 객체 안에서 `ts` 는
따옴표를 치고 `seqId` 는 안 친다. `json::parse_scalar_u64` 가 양쪽을 받는다.

### 체결이 배치로 온다 (OKX·바이비트)

한 프레임에 `data:[…]` 로 여러 건이 실린다. 레코드 하나에 체결 하나이므로 "프레임 하나 →
레코드 하나" 서명은 **어느 걸 버릴지 고르는 일**이 된다 (fractal-engine 은 마지막 것만
남겼다). 모든 체결이 시장을 움직였다.

그래서 `trades(payload)` 가 반복자를 주고 `decode_next(inst, recv_ns, out)` 이 슬롯 하나씩
채운다 — 링이 원하는 모양 그대로다(슬롯 잡고, 채우고, 발행하고, 반복).

### 한 구독이 두 kind 를 낼 수 있다 (OKX·바이비트)

OKX `books` 는 `action` 이 `snapshot`/`update`, 바이비트 `orderbook` 은 `type` 이
`snapshot`/`delta` 다. 그리고 **바이비트는 `u == 1` 이면 `type` 이 뭐라 하든 전체 교체다**
(오더북 서비스 재시작). 이건 소비자가 바이비트 규약을 몰라도 되게 핸들러가 결론을 내야 하는
자리다 — 와이어에는 *이건 통째 북이다* 라는 결론이 실린다.

모르는 값은 **`CryptoError::Unexpected`, 아무것도 발행하지 않는다.** 안전한 기본값이 없다:
델타를 스냅샷으로 읽으면 언급 안 된 레벨을 전부 버리고, 스냅샷을 델타로 읽으면 그 밖의
가격이 영영 안 지워진다.

### 빗썸은 업비트 디코더를 재수출한다

빗썸 v2 공개 WS 는 업비트와 필드까지 같다. 복사본을 두면 같이 고쳐야 하는데 안 고쳐진다
(위 "거래소를 크레이트로…" 와 같은 논리). **다른 건 베뉴 바이트뿐이고, 그건 모듈이 아니라
`Instrument` 가 정한다** — `bithumb::trade::decode` 에 업비트 `Instrument` 를 주면 업비트
레코드가 나온다. 모듈 경로는 문서고 인스트루먼트가 사실이다.

### 없는 시퀀스를 지어내지 않는다

업비트는 북 시퀀스도 갱신ID도 발행하지 않는다. fractal-engine 은 밀리초 타임스탬프를
`sequence_id` 자리에 넣었는데, 그러면 소비자가 같은 밀리초에 나온 두 북을 같은 북으로 본다.
`quote_ext` 는 `NONE` 으로 둔다. **없는 건 사실이고 대역은 사실이 아니다.**

### OKX·비트겟 `checksum` 은 싣지 않는다

`books` 메시지마다 상위 25단에 대한 CRC32 가 온다. 유용하지만 안 싣는다 — 핸들러는 북이
없어서 스스로 검증할 수 없고, 계산 규칙(25단, `가격:수량` 을 콜론으로 잇기)이 거래소 전용이라
소비자가 `venue` 로 분기해야 읽힌다. 되돌리는 건 싸다: `SnapshotDeltaPayload` 의 패딩과
`delta_flags` 비트 하나에 들어가고 다른 필드는 안 움직인다.

비트겟이 앞의 이유를 구체적으로 보여준다 — fractal-engine 의 검증기는 **북을 들고 있어야**
그 필드를 쓸 수 있다. 그게 피드 핸들러가 안 하는 바로 그 일이다.

### 시각의 단위가 거래소마다 다르다

대부분 밀리초지만 **쿠코인은 나노초**(`time`·`ts`, 19자리)이고 **게이트는 소수점 있는
밀리초**(`"1606292218213.4578"`)다. 그리고 쿠코인은 한 거래소 안에서도 갈린다 — 체결 채널은
나노초, 선물 북 델타(`timestamp`)와 현물 REST 북(`time`)은 밀리초다. **같은 `time` 이라는
이름으로.**

그래서 변환은 전부 `jeed_crypto::time` 한 곳에 있고, 디코더는 자기가 읽는 게 무엇인지
주석으로 밝힌다. 백만 배 틀리면 레코드가 1970년이나 서기 56000년에 찍힌다.

### REST 북도 디코드한다 — 다만 이 크레이트는 요청하지 않는다

게이트·쿠코인 현물의 델타 채널은 통째 북을 안 보낸다. 시작 북은 REST 로만 오고, 그걸
`snapshot::decode` 가 받는다. **소켓도 HTTP 클라이언트도 없다** — 바이트는 호출자가 준다.

그럼 왜 여기서 푸느냐: 안 그러면 소비자가 거래소 JSON 을 직접 파싱해야 하고, 그건 베뉴
지식을 링 반대편으로 넘기는 것이다. 프로세스를 나눈 이유가 없어진다.

### 변경 하나가 문자열로 오기도 한다 (쿠코인 선물)

`"change":"90631.2,sell,2"` — 가격·방향·수량이 쉼표로 이어진 한 줄이고 메시지당 레벨
하나다. 방향을 못 읽으면 **발행하지 않는다**: 반대쪽에 적용하면 두 가격이 틀리고
(움직였어야 할 것과 안 움직였어야 할 것) 어느 후속 메시지도 그걸 고쳐주지 않는다.

### `s` 를 검사한다

디코더 하나가 구독 하나라서 프레임의 심볼이 다르면 **배선 사고**다. 안 보고 넘기면 ETH
가격이 BTCUSDT 심볼을 달고 발행된다 — 가장 조용한 오류다. memcmp 한 번이니 검사한다.

거래소마다 어디 있는지가 다르다:

| | 어디에 |
|---|---|
| 바이낸스 · 바이비트 | `s` |
| 업비트 · 빗썸 | `code` / `cd` |
| OKX | `instId` — `books` 는 데이터 객체가 아니라 **`arg` 봉투에만** |
| 비트겟 | **`arg.instId` 뿐.** 데이터 객체에는 심볼이 아예 없다 |
| 게이트 | `result.s`(북) / `result.currency_pair`(체결) |
| 쿠코인 | `data.symbol`, 없으면 `topic` 의 콜론 뒤 — 선물 북은 **`topic` 뿐** |

봉투에만 있는 거래소(비트겟, 쿠코인 선물)는 프레임당 한 번 검사한다. 그래서 비트겟
`trade::trades` 는 OKX 와 달리 `Instrument` 를 받는다 — 검사할 기회가 거기밖에 없다.
(REST 본문엔 아예 없기도 하다. 있을 때만 본다.)

### 수신부는 `rustls` 만 들이고 WebSocket 프레이밍은 직접 쓴다

`jeed_crypto::recv` 가 워크스페이스의 **유일한 외부 의존**을 갖는다(`rustls` + `webpki-roots`,
`ring` 백엔드, `logging` 끔). `wss://` 가 TLS 라 피할 수 없다. `recv` 피처(기본 켜짐) 뒤에
있으므로 디코더만 쓰는 쪽은 `default-features = false`.

RFC 6455 프레이밍을 직접 쓴 이유는 **서버 프레임에 마스킹이 없어서**다. 페이로드가 수신
버퍼에 디코더가 원하는 그대로 놓이고 `Frame::payload` 는 그 슬라이스다 — 프레임당 복사도
할당도 없다. `tungstenite` 는 `read()` 가 `String` 을 준다. 마스킹은 우리가 보내는 쪽(구독,
pong)뿐이고 그건 초당 한 번도 안 된다.

- **확장은 안 내민다.** 그래서 `permessage-deflate` 가 협상될 수 없고, RSV 비트가 켜진 프레임은
  압축이 아니라 프로토콜 오류다. HTX(전 프레임 gzip)를 뺀 이유가 여기 코드로 있다
- **`Sec-WebSocket-Accept` 를 검증한다.** 그래서 SHA-1 이 60 줄 들어와 있다(`recv/ws/sha1.rs`).
  rustls 프로바이더는 SHA-1 을 안 내놓고, 해시 크레이트는 콜드 패스 한 호출에 의존성 하나다
- **조각난 메시지만 복사한다**(`ws::assemble`). 넘치면 잘라서 디코드하지 않고 끊는다 — 잘린
  JSON 은 얕은 북이 아니라 JSON 이 아니다

### `Router` 가 베뉴가 붙는 자리다

수신 루프는 어느 거래소인지 모른다. 메시지가 어느 스트림 것인지(바이낸스 `stream` 봉투,
바이비트 `topic`, OKX `arg`, 쿠코인 `topic:심볼`)와 거래소 방언 ping(`{"op":"ping"}`, `ping`,
`{"type":"ping"}`)이 거래소마다 다르고, 그 둘이 `Router::route` / `Router::keepalive` 다 —
`jeed-fix` 의 `MdAdapter` 와 같은 논리. 거래소별 라우터는 `<venue>/router.rs` 에 있고
`recv::VenueRouter` 가 베뉴 바이트로 고른다.

라우터는 **무엇을 보낼지 말하지 직접 보내지 않는다.** `subscriptions()`(연결될 때마다 보낼
구독 메시지), `rest_books()`(REST 로 받을 시작 북), `ticket()`(쿠코인 `bullet-public` — 소켓
주소 자체를 REST 로 받는다)은 전부 *요청*을 돌려주고 바이너리가 라운드 사이에 수행한다.
소켓·시계·HTTP 클라이언트를 라우터에 주지 않는 이유는 `jeed-fix` 가 `Reply` 를 돌려주는
이유와 같다 — 베뉴 대화 전체가 상대 없이 테스트된다.

생존은 두 층이다: RFC 6455 ping 은 모든 서버가 답하므로 루프가 보낸다(한 간격 침묵 → ping, 두
간격 → 끊는다. **어떤 프레임이든** 시계를 되돌린다 — 데이터가 오면 살아 있는 것이다). 방언
ping 은 라우터가 낸다. 바이낸스·업비트는 서버가 먼저 ping 하므로 후자가 필요 없다.

`recv::http` 는 요청 하나짜리 HTTP/1.1 클라이언트다(`Content-Length`·chunked·EOF). **수신 루프는
절대 부르지 않는다** — 바이너리가 열릴 때마다 한 번, 라운드 사이에 부른다. "연결 하나 = 루프
하나" 는 그대로다.

### 업비트는 binary 프레임에 지수 표기 숫자를 실어 보낸다

둘 다 실제 회선에서 확인한 것이다(2026-09-13). ① 업비트·빗썸은 JSON 을 **binary 프레임**으로
보낸다 — 루프는 binary 를 텍스트와 똑같이 라우터에 넘기고 `Stats::binary` 로 세기만 한다.
② 숫자가 천만을 넘으면 직렬화기가 지수 표기로 바꾼다: `"ask_price":1.04525E8`. `Instrument::price`
/`qty` 가 `json::plain_decimal` 로 자릿수를 **옮겨서**(계산이 아니라) 평범한 십진수로 편 뒤
읽는다. 다른 거래소 숫자는 `E` 스캔 한 번만 더 들고 그대로 통과한다.

## 바이너리를 다룰 때 반드시 아는 것

### conf 는 우리 TOML 리더가 읽는다 — 모르는 키는 기동 실패다

`jeed::toml` 은 두 conf 파일이 쓰는 부분집합(테이블·배열 테이블·인라인 테이블·문자열·정수·불·
여러 줄 배열)만 읽고 나머지(여러 줄 문자열, 날짜)는 **이름을 대고** 거부한다. `toml` 크레이트를
안 들인 이유는 문법이 아니라 의존성이다 — `serde` 와 열몇 개 패키지가 기동 시 한 번 읽는 파일
때문에 들어온다. 워크스페이스 외부 의존은 `rustls` 하나다.

읽는 쪽은 **모르는 키를 오류로** 낸다(`ring_slot` 이 기본값 옆에서 조용히 무시되면 안 된다).
그리고 **없는 가드는 켜진 것이다** — `[health]` 가 없으면 100 ms / 500 ms 가 들어가고, 끄려면
`0` 을 적어야 한다. 이미 한 번 난 사고다(feed_handler.md §9).

### 기동 검증이 잡는 것과 못 잡는 것

`KrxConf::validate` 가 거부하는 것: 포트 하나에 소켓 둘(피드 안이든 피드 사이든 — 윈도우는
둘 다 양쪽 그룹을 받아 **두 링에 전부 두 번** 발행한다), 코어 하나에 피드 둘, spin 피드에 코어
여럿(스레드가 옮겨 다니면 "핀된 코어 하나" 가 말뿐이다), spin 코어의 **SMT 형제**에 다른 피드,
2의 거듭제곱이 아닌 링, 링 이름 중복. 형제 판정은 OS 토폴로지에서 온다
(`GetLogicalProcessorInformationEx` / sysfs `thread_siblings_list`) — 못 읽으면 경고하고 그
규칙만 건너뛴다.

**못 잡는 것**: 소켓 ↔ trcode 대응. 어느 포트에 뭐가 실리는지는 회선 배정이다. 대신 리포트
줄이 "어느 소켓에서도 한 번도 안 본 trcode" 를 찍는다. 표준서 표에 없는 trcode 와 이 빌드에
디코더가 없는 trcode 는 **경고**다 — 표준서가 회선보다 늦고(`17F`), 마스터(`A0`)는 받아서
카운터로 확인할 일이 있어서다.

`jeed-krx conf/krx.toml --check` 는 아무것도 만들지 않고 여기까지만 한다. `--no-pin` 은 개발
기계용. 경로는 실행 디렉터리 기준이라 저장소 루트에서 띄운다.

### 실패할 수 있는 건 전부 스레드 전에 실패한다

링 생성과 소켓 가입은 메인 스레드에서 피드 순서대로 한다. 두 번째 피드의 그룹 주소가 틀리면
아무것도 도는 것 없이 종료한다 — 첫 피드가 이미 링에 쓰기 시작한 뒤가 아니라. 그 다음에야
`Receiver` 를 스레드로 옮기고 스레드가 스스로 핀한다. 피드 하나가 죽으면 전부 멈추고
종료 코드 3 이다 — 소비자가 반쪽 시장을 보게 두지 않는다.

리포트 줄은 하트비트와 같은 이유로 **수신 스레드가 자기 손으로** 찍는다(별도 스레드는 멈춘
루프를 계속 묘사한다). spin 루프는 256 라운드마다 시계를 본다 — 조용한 라운드는 이미
`poll_once` 안에서 시계를 읽으므로 그 비용이 아니다. 로그 크레이트·시그널 크레이트·affinity
크레이트도 없다. 전부 콜드 패스 몇백 줄이다.

배치 규칙(코어 중복·spin 코어 여럿·SMT 형제·링 크기·링 이름)은 `conf::rules` 한 곳이다 —
KRX 피드와 크립토 피드가 배치되는 방식은 같아서, 두 conf 가 `Placement` 로 같은 함수를 부른다.
소켓·trcode 규칙은 KRX conf 에, 종목·채널 규칙은 크립토 conf 에 남는다.

### 크립토 conf 는 네 단어로 말한다

`channels = ["trade", "bbo", "book", "delta"]`. 거래소 스트림 이름(`@depth@100ms`, `books`,
`orderbook.50.BTCUSDT`, `/market/level2:BTC-USDT`)은 라우터가 옮긴다. 거래소가 그 단어의
스트림을 안 주면 라우터 생성자가 거부하고, `validate` 가 그 생성자를 부르므로 `--check` 와
기동이 같은 답을 낸다. `book` 은 *그 거래소의 북 채널이 내는 것 전부*(바이낸스·OKX 는 통째,
바이비트·비트겟은 통째 뒤 디프)이고, `delta` 는 *디프만* 오는 채널이라 시작 북이 따로다 —
바이낸스·OKX 는 `book` 채널, 게이트·쿠코인은 REST.

`price_decimals`/`qty_decimals` 는 거래소 종목 참조에서 온 **설정**이다. 틀리면 자릿수를 못
담는 프레임이 버려지고(잘라 싣지 않는다) 리포트 줄의 `fail` 이 는다. 처음 다섯 건과 천 건마다
한 건은 원인과 프레임 앞부분이 로그에 찍힌다 — 실제 회선에서 업비트의 지수 표기와 바이낸스의
32단 초과 델타를 그걸로 찾았다.

### REST 는 라운드 사이에만 부른다

연결이 열릴 때마다(`Stats::opens` 가 움직일 때) 스레드가 구독 메시지를 보내고, 라우터가 달라는
시작 북을 REST 로 받아 `ingest_rest` 로 같은 링에 싣는다. 쿠코인은 연결 *전에* 티켓을 받아
`set_endpoint` 한다 — 끊길 때마다 다시(토큰이 만료된다). 블로킹 HTTP 한 번이 피드 스레드를
그 왕복만큼 세운다. 거래소 자신의 리싱크 절차("구독, 버퍼, 받기, 적용")가 요구하는 그 정지다.

## Coding Guidelines

### 와이어 레코드

- `#[repr(C)]` · 고정 크기 · 명시 패딩. **암묵 패딩 0** 을 컴파일 타임에 단언한다
  (필드 폭 합 == `size_of`). 그래야 `as_bytes()` 가 건전하다.
- `WIRE_FORMAT_VERSION` 을 올리는 것은 **양쪽 바이너리 동시 배포 이벤트**다. 가볍게 올리지 않는다.
- 와이어에는 **결론만 싣고 근거는 안 싣는다.** 소비자가 `venue` 로 분기해야 읽히는 비트는
  공통 필드가 아니다 (`STALE` 은 싣고, `VENUE_SEQ_GAP` 은 안 싣는다).
- 프로세스 로컬 식별자(인터닝 id)는 경계를 못 넘는다. 원본 `(venue, symbol)` 을 싣는다.

### shm 링 (`jeed-shm`)

- 링은 **백프레셔가 아니라 덮어쓰기**다. 꽉 찼을 때 새 레코드를 버리면 낡은 북을 지키고 방금
  시장을 움직인 체결을 버리는 셈이다. 생산자는 안 막히고, 소비자가 `producer_seq` 로 자기 유실을
  안다. `drop_counter` 는 **링에 넣기 전에** 버린 것만 센다.
- 레코드의 `producer_seq` 가 **슬롯 seqlock** 이다. 생산자는 쓰기 전에 `u64::MAX`(작업 중)를 박고,
  본문을 쓰고, Release 후 진짜 seq 를 마지막에 넣는다. 센티널을 빼면 반쯤 덮인 슬롯이 직전 바퀴의
  멀쩡해 보이는 seq 를 달고 읽힌다.
- `capacity` 는 2의 거듭제곱 강제 — 슬롯 인덱스가 나눗셈이 아니라 마스크가 된다.
- **슬롯을 commit 없이 떨구면 아무것도 발행되지 않는다.** 디코드 실패의 정답 반응이 이것이다.
- 소비자 `attach` 는 링의 끝에서 시작한다. 낡은 한 바퀴를 재생하지 않는다.

### Performance

- 핫 패스에서 힙 할당을 피한다. `Vec`/`Box`/`String` 대신 스택 배열·참조.
- 작은 핫 함수에 `#[inline]`. **`#[inline(always)]` 는 쓰지 않는다.**
- 16 바이트 이하 작은 타입은 `Copy`.
- 수신 루프는 코어에 핀된 busy-spin 이다. 루프 안에 블로킹 호출을 넣지 않는다.

### Time

- `UnixNano` 는 `u64` 라 `a - b` 가 랩한다. **시각 차는 전부 `saturating_sub`.**
- 시스템 시계는 **단조가 아니다.** 순서의 권위는 `producer_seq`, `recv_ns` 는 측정·라벨링용.
- KRX 원문 `매매처리시각` 은 `HHMMSSuuuuuu` 로 날짜가 없어 자정에 랩한다. 절대 ns 조립은
  피드 핸들러 안에서 끝낸다.

### Git

- **커밋 메시지는 영어로 쓴다.** 제목은 conventional commit(`feat(crypto): …`), 본문은 *왜*
  그렇게 했는지를 적는다 — 무엇을 바꿨는지는 diff 가 말한다. 코드 주석·문서가 한국어여도
  커밋 메시지는 영어다.
